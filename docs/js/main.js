'use strict';

// vainilla JavaScript
// const input = document.querySelector('input');
// const h1 = document.querySelector('h1');

// input.addEventListener('keyup', () => h1.innerHTML = input.value);
// directivas son atributos que potencializan el html
new Vue({
    el: 'main',
    data: {
        mensaje: ''
    }
});